const library = [];

function Book(title, author, pages) {
  // this makes the object thats it
  this.title = title;
  this.author = author;
  this.pages = pages;
}

function addBook() {
  library.push(book);
}

console.log('hello');
